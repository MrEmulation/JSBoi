// src/MMU.js

import Memory from './Memory';

class MMU {
    constructor() {
        this.memory = new Memory();
    }

    readByte(address) {
		
    }

    readWord(address) {
		
    }

    writeByte(address, byte) {
		
    }

    writeWord(address, byte) {
		
    }
}

export default MMU;
